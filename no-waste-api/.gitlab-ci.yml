image: node:latest

stages:
  - deploy

deploy:
  type: deploy
  stage: deploy
  script:
    - cd 'no-waste-api'
    - apt-get update -qy
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=no-waste-api --api-key=$HEROKU_API_KEY
  only:
    - master
